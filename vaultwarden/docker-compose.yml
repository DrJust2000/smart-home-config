services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    environment:
      - SIGNUPS_ALLOWED=true  # Erlaubt dir, deinen Account zu erstellen
    volumes:
      - ./vw-data:/data
    ports:
      - 8080:80